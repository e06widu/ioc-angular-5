import { CountArrayFilterPipe } from './count-array-filter.pipe';

describe('CountArrayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountArrayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
