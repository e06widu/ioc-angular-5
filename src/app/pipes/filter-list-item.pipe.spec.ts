import { FilterListItemPipe } from './filter-list-item.pipe';

describe('FilterListItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListItemPipe();
    expect(pipe).toBeTruthy();
  });
});
