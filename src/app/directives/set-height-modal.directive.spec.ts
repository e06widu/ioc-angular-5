import { SetHeightModalDirective } from './set-height-modal.directive';

describe('SetHeightModalDirective', () => {
  it('should create an instance', () => {
    const directive = new SetHeightModalDirective();
    expect(directive).toBeTruthy();
  });
});
